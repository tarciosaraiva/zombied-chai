language: node_js
addons:
  code_climate:
    repo_token: 9781c5da8df01dc106ce20a685ef577563db4f4b0dcdf4d5a9132499c945ea8a
after_success:
  - git config --global user.email "builds@travis-ci.com"
  - git config --global user.name "Travis CI"
  - export VERSION_NUMBER=1.0.$TRAVIS_BUILD_NUMBER
  - npm version "$VERSION_NUMBER"
  - git push -f -q https://tarciosaraiva:$GITHUB_API_KEY@github.com/tarciosaraiva/zombied-chai --tags
after_script:
  - codeclimate-test-reporter < ./coverage/lcov.info
